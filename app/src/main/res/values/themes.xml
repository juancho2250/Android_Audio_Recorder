<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyAudioRecorder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyAudioRecorder" parent="Base.Theme.MyAudioRecorder" />

    <style name="SearchInput" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="boxBackgroundColor">@color/fill</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeWidthFocused">0dp</item>
        <item name="boxCornerRadiusBottomEnd">20dp</item>
        <item name="boxCornerRadiusBottomStart">20dp</item>
        <item name="boxCornerRadiusTopEnd">20dp</item>
        <item name="boxCornerRadiusTopStart">20dp</item>
        <item name="endIconMode">clear_text</item>
        <item name="startIconDrawable">@drawable/ic_search_24</item>
    </style>
</resources>